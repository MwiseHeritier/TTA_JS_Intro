const name =['mugisha', 'mugabo','manzi' , 'ishimwe', 'rukundo'];
name.push("alice") // add alice at the end of the array
name.unshift("heritier"); // add heritier at the start of the array
name.pop(); // removes the last element in the arrray
name.shift(); // removes the first element in the array
name.delete[3];
console.log(name);



